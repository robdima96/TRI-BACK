# Raw corpora (not committed)

Download here, then point `screen_medqa.py --path` at the file.

**Primary (full English qbank):** Hugging Face `GBaker/MedQA-USMLE-4-options` via `--source hf` (no file needed).

**Optional AgentClinic MedQA OSCE (still MedQA, not NEJM):**

https://raw.githubusercontent.com/SamuelSchmidgall/AgentClinic/main/agentclinic_medqa.jsonl

Do not download `agentclinic_nejm*.jsonl` or MIMIC files into this folder for sampling.
